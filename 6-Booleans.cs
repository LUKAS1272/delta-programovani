namespace Booleans;

class Program
{
    static void Main(string[] args)
    {
        // V C# i jiných programovacích jazycích existuje i další datový typ - boolean
        // Pro definici proměnné používáme bool
        // Boolean může mít pouze 2 hodnoty

        // true - pravda
        bool isDay = true;
        
        // false - nepravda
        bool isNight = false;

        // Stejně jako s ostatními datovými typy, i boolean můžeme vypsat do konzole
        Console.WriteLine(isDay); // true (pravda)
        Console.WriteLine(inNight); // false (nepravda)
    }
}